/**
 * @file
 * Global utilities.
 *
 */
(function ($, Drupal, once) {

  'use strict';

  Drupal.behaviors.stla = {
    attach: function (context, settings) {
    
    }
  };

})(jQuery, Drupal, once);
